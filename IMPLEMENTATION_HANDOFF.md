# Implementation Handoff

The initial browser-based scaffold has been retained only as a documented full-stack reference. The active HomeOS India product target is now a native mobile application using the platform's Expo mobile template. Customer and technician workflows will be built there first, backed by the same database-first domain model. The browser remains appropriate solely for the internal operations console.
